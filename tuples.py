# Tuples are similar to lists, the big difference is that they are not mutable.

namestuple = ('leon', 'edmundo', 'temer','peter')
print(namestuple[1])

# basically you do the same thing as a list, but you can't assign or remove values;